const sendOtp = async (whatsappNumber, otpCode) => {
    // In a real app, you would implement the OTP sending logic here.
    // For now, we'll just log the OTP to the console.
    console.log(`Sending OTP ${otpCode} to ${whatsappNumber}`);
    };

    module.exports = {
    sendOtp,
    };
